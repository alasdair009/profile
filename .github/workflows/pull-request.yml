name: Pull request

on:
  pull_request:
    branches:
      - main
      - rc/**

jobs:
  static-analysis:
    uses: ./.github/workflows/static-analysis.yml
  test:
    uses: ./.github/workflows/chromatic.yml

import { Meta, Typeset } from "@storybook/addon-docs/blocks";
import { fontFamilies, fontSizes, fontWeights } from "../../../styles/tokens";

<Meta title="Design Tokens/Typography" />

# Typography

The system uses the same font in all areas with associated fallbacks: <code>{fontFamilies.fontInter}</code>.

<Typeset
  fontSizes={[fontSizes.small, fontSizes.medium, fontSizes.large]}
  fontWeight={fontWeights.regular}
  sampleText={"Font at regular weight"}
  fontFamily={fontFamilies.fontInter}
  style={{ color: "var(--color-white-ghost)" }}
/>

<Typeset
  fontSizes={[fontSizes.small, fontSizes.medium, fontSizes.large]}
  fontWeight={fontWeights.bold}
  sampleText={"Font at bold weight"}
  fontFamily={fontFamilies.fontInter}
  style={{ color: "var(--color-white-ghost)" }}
/>

import { Meta, Typeset } from "@storybook/addon-docs/blocks";
import { fonts, fontSizes, fontWeights } from "./typography";
import { colors } from "../colors";

<Meta title="Design Tokens/Typography" />

# Typography

The system uses the same font in all areas with associated fallbacks: <code>{fonts.body}</code>.

<Typeset
  fontSizes={[fontSizes.small.raw, fontSizes.medium.raw, fontSizes.large.raw]}
  fontWeight={fontWeights.regular}
  sampleText={"Font at regular weight"}
  fontFamily={fonts.body}
  style={{ color: colors.whiteGhost }}
/>

<Typeset
  fontSizes={[fontSizes.small.raw, fontSizes.medium.raw, fontSizes.large.raw]}
  fontWeight={fontWeights.bold}
  sampleText={"Font at bold weight"}
  fontFamily={fonts.body}
  style={{ color: colors.whiteGhost }}
/>

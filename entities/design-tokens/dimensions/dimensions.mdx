import { Meta } from "@storybook/addon-docs/blocks";
import { DimensionViewer } from "../../../lib/storybook/DimensionViewer";
import { breakpoints, globalSizes, sizes } from "../../../styles/tokens";

<Meta title="Design Tokens/Dimensions" />

# Dimensions

## Sizes

Each size key has properties for the dimension in number (`raw`), `px` and `rem`.

<DimensionViewer
  units={sizes}
  barUnits={Object.entries(sizes)}
  suffix="px"
  title="sizes"
  sourceExample="width: var(--size-s8);"
  objectName="sizes"
/>

## Global Sizes

Larger sizes used to maintain consistent sizes across entities.

<DimensionViewer
  units={globalSizes}
  barUnits={Object.entries(globalSizes)}
  suffix="px"
  title="globalSizes"
  sourceExample="width: var(--global-content-max-width);"
  objectName="globalSizes"
/>

## Breakpoints

These are used to adjust the content layout for different device screen widths.

<DimensionViewer
  units={breakpoints}
  barUnits={breakpoints}
  title="breakpoints"
  suffix="px"
  sourceExample="@media (--breakpoint-medium){...}"
  objectName="breakpoints"
/>
